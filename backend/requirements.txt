# =============================================================================
# J.A.R.V.I.S. Backend — Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# =============================================================================

# -- Web framework ------------------------------------------------------------
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=0.0.12          # file uploads / form data

# -- Database: PostgreSQL (async) ---------------------------------------------
sqlalchemy[asyncio]>=2.0.36,<3.0
asyncpg>=0.30.0,<1.0
alembic>=1.14.0,<2.0

# -- Database: Redis (async) --------------------------------------------------
redis[hiredis]>=5.2.0,<6.0

# -- Database: Neo4j (knowledge graph) ----------------------------------------
neo4j>=5.27.0,<6.0

# -- Database: Qdrant (vector store) ------------------------------------------
qdrant-client>=1.12.0,<2.0

# -- Pydantic / settings ------------------------------------------------------
pydantic[email]>=2.10.0,<3.0
pydantic-settings>=2.6.0,<3.0

# -- Auth / Security -----------------------------------------------------------
python-jose[cryptography]>=3.3.0,<4.0
passlib[bcrypt]>=1.7.4,<2.0
bcrypt>=4.0.0,<5.0                       # passlib incompatible with bcrypt 5.x
cryptography>=44.0.0,<45.0
webauthn>=2.0.0,<3.0                     # WebAuthn / passkey support
pyotp>=2.9.0,<3.0                        # TOTP 2FA

# -- HTTP clients --------------------------------------------------------------
httpx>=0.28.0,<1.0
aiohttp>=3.11.0,<4.0

# -- Timezone data (needed by zoneinfo on minimal Linux containers) -----------
tzdata>=2024.1

# -- Logging -------------------------------------------------------------------
structlog>=24.4.0,<25.0

# -- LLM providers -------------------------------------------------------------
openai>=1.57.0,<2.0                     # SDK used by Perplexity, GLM, Stark Protocol (OpenAI-compatible APIs)
anthropic>=0.40.0,<1.0
google-genai>=1.0.0

# -- Google Workspace (Gmail, Calendar, Drive, Sheets) -------------------------
google-api-python-client>=2.100.0,<3.0
google-auth-oauthlib>=1.2.0,<2.0
google-auth-httplib2>=0.2.0,<1.0

# -- Agent orchestration -------------------------------------------------------
langgraph>=0.2.0,<1.0

# -- TTS (text-to-speech) fallback ---------------------------------------------
edge-tts>=6.1.0,<7.0
gTTS>=2.5.0,<3.0

# -- Document processing -------------------------------------------------------
pypdf>=5.1.0,<6.0
beautifulsoup4>=4.12.0,<5.0

# -- NLP / NER ----------------------------------------------------------------
spacy>=3.8.0,<4.0

# -- Embeddings (optional, for local models) -----------------------------------
# sentence-transformers>=3.3.0,<4.0  # uncomment if using local embeddings

# -- Image processing ---------------------------------------------------------
Pillow>=11.0.0,<12.0

# -- Twilio (voice calls) ----------------------------------------------------
twilio>=9.0.0,<10.0

# -- Email (outbound via Resend) ---------------------------------------------
resend>=2.0.0,<3.0

# -- vCard parsing (contacts import) -----------------------------------------
vobject>=0.9.7,<1.0
